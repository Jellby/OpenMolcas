dist: trusty
compiler: gcc
os: linux
git:
  submodules: false

addons:
  apt:
    packages:
    - gfortran

before_script:
- git submodule update --init External/lapack
script:
- mkdir build ; cd build
- cmake .. ; make
